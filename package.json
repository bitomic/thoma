{
  "name": "sacarosa",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build:all": "yarn workspaces foreach -pvt run build",
    "lint:all": "yarn workspaces foreach -pv run lint",
    "lintfix:all": "yarn workspaces foreach -pv run lint:fix",
    "prepare": "husky install .github/husky",
    "amqp": "yarn workspace @sacarosa/amqp",
    "discord": "yarn workspace @sacarosa/discord",
    "shared": "yarn workspace @sacarosa/shared"
  },
  "devDependencies": {
    "@commitlint/cli": "^16.2.1",
    "@commitlint/config-conventional": "^16.2.1",
    "@typescript-eslint/eslint-plugin": "^5.12.1",
    "@typescript-eslint/parser": "^5.12.1",
    "eslint": "^8.10.0",
    "husky": "^7.0.4",
    "lint-staged": "^12.3.4",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.2.0"
}
